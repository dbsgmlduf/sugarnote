import 'package:flutter/material.dart';

class Function3Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('기능 3 대기 중입니다.'),
    );
  }
}
